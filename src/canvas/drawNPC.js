// NPC rendering — outdoor NPCs are rendered via drawPlayer with default params.
// This module is reserved for future dedicated NPC sprite/dialogue rendering.
export { drawPlayer as drawNPC } from './drawPlayer';
